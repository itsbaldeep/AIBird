// Gravity directly relates to the speed that bird falls to the ground
const gravity = 3;

// Pillar width is the size of the width of pillars
const pillarWidth = 40;

// The gap between top and bottom pillars
const pillarGap = 150;

// Speed of the game
const gameSpeed = 1.8;
